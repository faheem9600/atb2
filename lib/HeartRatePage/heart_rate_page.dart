import 'package:flutter/material.dart';

class HeartPage extends StatelessWidget{
  @override 
  Widget build(BuildContext context){
    return Container(
      child: Text("Heart Rate Page", textScaleFactor: 2.0),
    );
  }
}