import 'package:flutter/material.dart';

class KaloriPage extends StatelessWidget{
  @override 
  Widget build(BuildContext context){
    return Container(
      child: Text("Kalori", textScaleFactor: 2.0),
    );
  }
}